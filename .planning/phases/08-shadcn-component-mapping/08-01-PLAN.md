---
phase: 08-shadcn-component-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/shadcn/types.ts
  - src/main/shadcn/tokens.ts
  - src/main/shadcn/specs/button.ts
  - src/main/shadcn/specs/card.ts
  - src/main/shadcn/specs/badge.ts
  - src/main/shadcn/specs/input.ts
  - src/main/shadcn/specs/index.ts
autonomous: true

must_haves:
  truths:
    - "Shadcn design tokens define all color and sizing values in one place"
    - "Component specs define variant and size styles for Button, Card, Badge, Input"
    - "Types enforce correct structure for component specifications"
  artifacts:
    - path: "src/main/shadcn/types.ts"
      provides: "ComponentSpec, VariantStyle, SizeStyle interfaces"
      exports: ["ComponentSpec", "VariantStyle", "SizeStyle", "ResolvedStyles"]
    - path: "src/main/shadcn/tokens.ts"
      provides: "Centralized Shadcn design tokens"
      exports: ["SHADCN_COLORS", "SHADCN_RADII", "SHADCN_SIZES"]
    - path: "src/main/shadcn/specs/index.ts"
      provides: "COMPONENT_SPECS map for all component specifications"
      exports: ["COMPONENT_SPECS"]
  key_links:
    - from: "src/main/shadcn/specs/button.ts"
      to: "src/main/shadcn/tokens.ts"
      via: "imports SHADCN_COLORS, SHADCN_SIZES"
      pattern: "import.*SHADCN_COLORS.*tokens"
---

<objective>
Create Shadcn design tokens and component specifications as TypeScript objects

Purpose: Establish the foundation for Shadcn-styled Figma node generation by defining all visual properties (colors, radii, padding, sizes) for Button, Card, Badge, and Input components with their variants.

Output: Type definitions, centralized tokens, and component specs ready for the ComponentFactory in Plan 02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-shadcn-component-mapping/08-RESEARCH.md
@src/main/generator/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Shadcn types and tokens</name>
  <files>src/main/shadcn/types.ts, src/main/shadcn/tokens.ts</files>
  <action>
Create src/main/shadcn/ directory with:

1. types.ts - Define interfaces:
   - ComponentSpec: name, defaultVariant, defaultSize, baseStyles, variants, sizes
   - VariantStyle: backgroundColor, textColor, borderColor, borderWidth, shadow
   - SizeStyle: height, paddingX, paddingY, fontSize, borderRadius, iconSize
   - BaseStyles: fontWeight, textAlign
   - ShadowSpec: color, offsetX, offsetY, blur, spread
   - ResolvedStyles: merged styles ready to apply to Figma nodes

2. tokens.ts - Define constants (from RESEARCH.md):
   - SHADCN_COLORS: { primary, primaryForeground, secondary, secondaryForeground, destructive, muted, mutedForeground, background, foreground, border, input }
   - SHADCN_RADII: { none, sm, md, lg, full }
   - SHADCN_SIZES: { xs, sm, default, lg, icon } with height/padding/fontSize/radius

All colors in hex format from research: primary=#18181B, destructive=#EF4444, etc.
  </action>
  <verify>TypeScript compiles without errors: `cd /Users/dwayne/Figma-plugin && npx tsc --noEmit`</verify>
  <done>types.ts exports all interfaces, tokens.ts exports all design token constants</done>
</task>

<task type="auto">
  <name>Task 2: Create component specifications</name>
  <files>src/main/shadcn/specs/button.ts, src/main/shadcn/specs/card.ts, src/main/shadcn/specs/badge.ts, src/main/shadcn/specs/input.ts, src/main/shadcn/specs/index.ts</files>
  <action>
Create src/main/shadcn/specs/ directory with component spec files:

1. button.ts - Export BUTTON_SPEC: ComponentSpec
   - variants: default, secondary, outline, ghost, destructive, link
   - sizes: default, sm, lg, icon
   - Use SHADCN_COLORS and SHADCN_SIZES from tokens
   - baseStyles: fontWeight 500, textAlign center

2. card.ts - Export CARD_SPEC: ComponentSpec
   - variants: default (with border and shadow)
   - sizes: default (padding 24px, radius 8px)
   - baseStyles: backgroundColor background

3. badge.ts - Export BADGE_SPEC: ComponentSpec
   - variants: default, secondary, outline, destructive
   - sizes: default (smaller padding, rounded-full/9999 radius)
   - baseStyles: fontWeight 500, fontSize 12

4. input.ts - Export INPUT_SPEC: ComponentSpec
   - variants: default (with border)
   - sizes: default, sm, lg
   - baseStyles: border 1px, radius 6px

5. index.ts - Export COMPONENT_SPECS: Map<string, ComponentSpec>
   - Map Button, Card, Badge, Input to their specs
   - Type as Record<ShadcnComponentType, ComponentSpec | undefined> for type safety
  </action>
  <verify>TypeScript compiles: `cd /Users/dwayne/Figma-plugin && npx tsc --noEmit`</verify>
  <done>COMPONENT_SPECS map contains Button, Card, Badge, Input specs with all variants and sizes defined</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. COMPONENT_SPECS.get('Button') returns valid ComponentSpec with 6 variants and 4 sizes
3. SHADCN_COLORS.primary === '#18181B'
4. SHADCN_SIZES.default.height === 40
</verification>

<success_criteria>
- All type definitions compile without errors
- Design tokens match Shadcn reference values from research
- Button spec has default, secondary, outline, ghost, destructive, link variants
- Card, Badge, Input specs have appropriate variants and sizes
- COMPONENT_SPECS map exports all four component specs
</success_criteria>

<output>
After completion, create `.planning/phases/08-shadcn-component-mapping/08-01-SUMMARY.md`
</output>
