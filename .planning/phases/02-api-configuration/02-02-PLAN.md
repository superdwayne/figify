---
phase: 02-api-configuration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ui/hooks/useApiKey.ts
  - src/ui/components/Settings.tsx
  - src/ui/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can enter API key in settings panel"
    - "User can see masked API key when one is stored"
    - "User can toggle API key visibility (show/hide)"
    - "User can update existing API key"
    - "User can clear stored API key"
    - "Settings panel is accessible from main UI"
  artifacts:
    - path: "src/ui/hooks/useApiKey.ts"
      provides: "Hook for API key state and storage operations"
      exports: ["useApiKey"]
    - path: "src/ui/components/Settings.tsx"
      provides: "Settings panel with API key input"
      exports: ["Settings"]
    - path: "src/ui/App.tsx"
      provides: "View switching between main content and settings"
      contains: "Settings"
  key_links:
    - from: "src/ui/hooks/useApiKey.ts"
      to: "postToPlugin"
      via: "REQUEST messages with storage actions"
      pattern: "action.*API_KEY"
    - from: "src/ui/components/Settings.tsx"
      to: "src/ui/hooks/useApiKey.ts"
      via: "useApiKey hook import"
      pattern: "useApiKey"
    - from: "src/ui/App.tsx"
      to: "src/ui/components/Settings.tsx"
      via: "conditional render based on view state"
      pattern: "view.*settings"
---

<objective>
Create the Settings UI component and useApiKey hook that allow users to manage their Anthropic API key through the message-based storage protocol.

Purpose: Users need a way to enter, view (masked), update, and clear their API key. The BYOK model requires this for Claude API access.

Output: Working Settings panel accessible from the main UI, with password input, show/hide toggle, save, and clear functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-configuration/02-RESEARCH.md
@.planning/phases/02-api-configuration/02-01-SUMMARY.md
@src/shared/messages.ts
@src/ui/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useApiKey hook for storage operations</name>
  <files>src/ui/hooks/useApiKey.ts</files>
  <action>
Create a custom hook that encapsulates API key state and message-based storage operations.

1. Create the hook with this interface:
   ```typescript
   interface UseApiKeyReturn {
     apiKey: string | null;
     isLoading: boolean;
     isSaving: boolean;
     error: string | null;
     loadApiKey: () => Promise<void>;
     saveApiKey: (key: string) => Promise<boolean>;
     clearApiKey: () => Promise<boolean>;
   }
   ```

2. Implement using the existing message pattern from App.tsx:
   - Use `parent.postMessage({ pluginMessage: ... }, '*')` to send messages
   - Set up message listener for RESPONSE messages with correlation ID tracking
   - Return promise that resolves when response arrives

3. Key implementation details:
   - On mount, call `loadApiKey()` to fetch stored key
   - `saveApiKey` validates key starts with `sk-ant-` before sending
   - Track loading/saving states to prevent double submissions
   - Handle errors gracefully (return false on failure, set error state)

4. Add validation helper:
   ```typescript
   function isValidApiKeyFormat(key: string): boolean {
     return key.startsWith('sk-ant-') && key.length >= 50;
   }
   ```

Use the correlation ID pattern already established in the codebase. Import `generateCorrelationId` from shared/messages.ts.
  </action>
  <verify>Run `npm run build` - TypeScript should compile without errors.</verify>
  <done>useApiKey hook exports loadApiKey, saveApiKey, clearApiKey with proper loading states and error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Create Settings component with API key form</name>
  <files>src/ui/components/Settings.tsx</files>
  <action>
Create Settings component with API key input form following the existing UI patterns (Tailwind CSS).

1. Component structure:
   - Header with "Settings" title and back button (calls onClose prop)
   - API key input section with:
     - Label: "Anthropic API Key"
     - Password input (type togglable between 'password' and 'text')
     - Show/hide toggle button with Eye/EyeOff icons (use lucide-react)
     - Help text: "Get your API key from console.anthropic.com"
   - Action buttons:
     - "Save" button (primary style, disabled when saving or invalid input)
     - "Clear" button (secondary/destructive style, only shown when key exists)

2. Component props:
   ```typescript
   interface SettingsProps {
     onClose: () => void;
   }
   ```

3. Internal state:
   - `inputValue`: string (the input field value)
   - `showKey`: boolean (toggle password visibility)

4. Behavior:
   - On mount, populate `inputValue` with existing apiKey (if any)
   - On save: validate format, call saveApiKey, show success/error feedback
   - On clear: call clearApiKey, clear input, show confirmation
   - Show loading spinner on Save button when isSaving

5. Styling (match existing Tailwind patterns in App.tsx):
   - Use `bg-secondary`, `hover:bg-secondary/80` for secondary buttons
   - Use `bg-primary text-primary-foreground` for primary button
   - Use `text-destructive` for clear button
   - Input: `border rounded-md px-3 py-2 w-full`

6. Validation feedback:
   - If input doesn't start with `sk-ant-`, show inline error below input
   - Disable Save button until valid format
  </action>
  <verify>Run `npm run build` - TypeScript should compile. No runtime errors in console.</verify>
  <done>Settings component renders API key form with input, show/hide toggle, save, and clear buttons with proper validation.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Settings into App with view switching</name>
  <files>src/ui/App.tsx</files>
  <action>
Add view state to App.tsx to switch between main content and settings panel.

1. Add view state:
   ```typescript
   const [view, setView] = useState<'main' | 'settings'>('main');
   ```

2. Add settings button to header (next to status badge):
   - Use Settings icon from lucide-react (gear icon)
   - On click: `setView('settings')`
   - Style: `p-1 hover:bg-secondary rounded`

3. Conditional rendering in main content area:
   ```typescript
   {view === 'settings' ? (
     <Settings onClose={() => setView('main')} />
   ) : (
     <ImageCapture />
   )}
   ```

4. Import the Settings component:
   ```typescript
   import { Settings } from './components/Settings';
   ```

5. Import Settings icon:
   ```typescript
   import { Settings as SettingsIcon } from 'lucide-react';
   ```

Keep the existing footer with debug info visible in both views.
  </action>
  <verify>Run `npm run build` - TypeScript should compile. Load plugin in Figma, verify settings icon appears in header, clicking opens Settings view, back button returns to main view.</verify>
  <done>App.tsx has view state, settings button in header, conditional rendering of Settings or ImageCapture based on view.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. Plugin loads in Figma showing settings gear icon in header
3. Clicking gear opens Settings panel
4. Enter API key starting with `sk-ant-` + 50+ chars, click Save - no errors
5. Close and reopen plugin - API key persists (shows masked)
6. Click Clear - key is removed
7. Toggle show/hide - key visibility changes
</verification>

<success_criteria>
- User can enter API key in settings panel
- API key persists across Figma sessions
- User can update or clear stored API key
- API key input has show/hide toggle
- Settings accessible via gear icon in header
- Validation prevents saving invalid key format
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-configuration/02-02-SUMMARY.md`
</output>
