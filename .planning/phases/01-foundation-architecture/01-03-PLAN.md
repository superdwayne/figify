---
phase: 01-foundation-architecture
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/main.ts
  - src/ui/App.tsx
  - dist/main.js
  - dist/ui.html
autonomous: false

must_haves:
  truths:
    - "pnpm build produces dist/main.js and dist/ui.html"
    - "TypeScript compiles with zero errors in strict mode"
    - "Plugin loads in Figma without console errors"
    - "Main thread sends INIT message when UI reports ready"
    - "UI displays Connected status after receiving INIT"
    - "Test Connection button triggers ping/pong round trip"
  artifacts:
    - path: "dist/main.js"
      provides: "Bundled sandbox code"
      min_lines: 10
    - path: "dist/ui.html"
      provides: "Bundled UI with inlined CSS/JS"
      contains: "<!DOCTYPE html>"
  key_links:
    - from: "src/main.ts"
      to: "dist/main.js"
      via: "Vite build"
      pattern: "showUI"
    - from: "src/ui/App.tsx"
      to: "dist/ui.html"
      via: "Vite build + singlefile"
      pattern: "Screenshot to Shadcn"
---

<objective>
Verify the complete dual-thread architecture works end-to-end: build produces valid Figma plugin bundles, TypeScript compiles, and the plugin loads and communicates correctly in Figma.

Purpose: This is the integration verification that proves Phase 1 success criteria are met. Without this, we cannot confidently move to Phase 2.
Output: Working build artifacts (dist/main.js, dist/ui.html) and verified plugin functionality in Figma.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-architecture/01-CONTEXT.md
@.planning/phases/01-foundation-architecture/01-01-SUMMARY.md
@.planning/phases/01-foundation-architecture/01-02-SUMMARY.md
@src/main.ts
@src/ui/App.tsx
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify output artifacts</name>
  <files>
    dist/main.js
    dist/ui.html
  </files>
  <action>
Run the build and verify output:

1. Run `pnpm build` - should complete without errors

2. Verify dist/main.js exists and contains:
   - figma.showUI call
   - Message handling code
   - No DOM or React code (sandbox cannot use these)

3. Verify dist/ui.html exists and contains:
   - Complete HTML document
   - Inlined CSS (Tailwind styles)
   - Inlined JavaScript (React app)
   - No external script/link tags (all inlined by vite-plugin-singlefile)

4. Run `pnpm typecheck` one final time to confirm zero errors

5. Check bundle sizes are reasonable:
   - main.js should be small (< 10KB) - just Figma API calls
   - ui.html can be larger (< 500KB) - includes React + Tailwind

If build fails:
- Check vite.config.ts has correct entry points
- Check tsconfig.json paths are correct
- Ensure all imports resolve

If typecheck fails:
- Fix any type errors
- Ensure strict mode is enabled
  </action>
  <verify>
Run `pnpm build` - exits with code 0.
Run `ls -la dist/` - shows main.js and ui.html.
Run `pnpm typecheck` - exits with code 0.
  </verify>
  <done>
dist/main.js and dist/ui.html exist. Build completes without errors. TypeScript strict mode passes. Bundle sizes are reasonable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Figma plugin with dual-thread architecture:
- Sandbox main thread (dist/main.js) that launches UI and handles messages
- React UI shell (dist/ui.html) with Shadcn styling and message handling
- Typed message protocol with correlation IDs
  </what-built>
  <how-to-verify>
1. Open Figma desktop app (must be desktop, not web)
2. Go to Menu > Plugins > Development > Import plugin from manifest...
3. Select the manifest.json file from this project directory
4. Run the plugin: Plugins > Development > Screenshot to Shadcn
5. Verify the UI window opens with:
   - "Screenshot to Shadcn" header
   - Status badge showing "Connected" (green)
   - Placeholder image upload area
   - Selection count in footer
   - "Test Connection" and "Close" buttons
6. Click "Test Connection" button
   - Should update "Last ping: [timestamp]" in footer
   - This proves main <-> UI round-trip communication works
7. Select some objects on the canvas
   - Footer should update "Selection: N nodes"
8. Click "Close" button
   - Plugin window should close
9. Check Figma's Console (Plugins > Development > Show/Hide console)
   - Should show "UI ready, sending init" log
   - Should NOT show any errors

Expected result: All 9 steps pass without errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all verification steps pass, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. `pnpm build` succeeds
2. `pnpm typecheck` passes with strict mode
3. dist/main.js and dist/ui.html exist
4. Plugin loads in Figma without errors
5. UI shows "Connected" status
6. Test Connection ping/pong works
7. Selection changes are reflected in UI
8. Close button works
</verification>

<success_criteria>
Phase 1 Success Criteria (from roadmap):
- [x] Plugin loads in Figma without errors
- [x] Main thread can send message to UI thread and receive response
- [x] UI thread renders React application with plugin shell
- [x] TypeScript compilation succeeds with strict mode enabled
- [x] Build produces valid Figma plugin bundle (manifest.json valid)

All criteria verified by human testing in Figma.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-architecture/01-03-SUMMARY.md`
</output>
