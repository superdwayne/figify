---
phase: 03-image-input
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/ui/components/ImageCapture.tsx
  - src/ui/App.tsx
autonomous: false

must_haves:
  truths:
    - "User can paste screenshot via Cmd/Ctrl+V and see it in plugin"
    - "User can drag-drop image file onto plugin window"
    - "Plugin displays preview of captured image before processing"
    - "Plugin validates image format (PNG, JPG, WebP) and shows error for invalid types"
    - "Drop zone shows visual feedback when dragging over it"
  artifacts:
    - path: "src/ui/components/ImageCapture.tsx"
      provides: "React component for image capture drop zone with preview"
      exports: ["ImageCapture"]
      min_lines: 50
    - path: "src/ui/App.tsx"
      provides: "Main app component with ImageCapture integrated"
      contains: "ImageCapture"
  key_links:
    - from: "src/ui/components/ImageCapture.tsx"
      to: "src/ui/hooks/useImageCapture.ts"
      via: "import and use hook"
      pattern: "import.*useImageCapture"
    - from: "src/ui/App.tsx"
      to: "src/ui/components/ImageCapture.tsx"
      via: "import and render component"
      pattern: "import.*ImageCapture"
---

<objective>
Create ImageCapture UI component and integrate into App.tsx to complete the user-facing image input functionality.

Purpose: Completes Phase 3 - users can now paste screenshots or drag-drop images into the plugin, see a preview, and receive validation feedback. This is the visible feature that enables the AI analysis workflow in later phases.

Output: Working ImageCapture component integrated into main app, with visual drag state, image preview, error display, and clear functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-image-input/03-RESEARCH.md
@.planning/phases/03-image-input/03-01-SUMMARY.md
@src/ui/hooks/useImageCapture.ts
@src/ui/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ImageCapture component</name>
  <files>src/ui/components/ImageCapture.tsx</files>
  <action>
Create React component that uses the useImageCapture hook from Plan 01.

**Component structure:**

```typescript
import { useImageCapture } from '../hooks/useImageCapture';

export function ImageCapture() {
  const { capturedImage, isDragging, error, clearImage, dropZoneProps } = useImageCapture();

  // Render logic...
}
```

**Visual requirements:**

1. **Drop zone container** (receives dropZoneProps):
   - Use Tailwind classes matching existing App.tsx style
   - Border: dashed, rounded-lg
   - Padding: p-8
   - Text centered
   - Transition for smooth state changes
   - **isDragging state:** border-blue-500, bg-blue-50/50
   - **error state:** border-red-500
   - **default state:** border-gray-300 (use border-muted in theme)

2. **Empty state (no capturedImage):**
   - Show icon (use same SVG pattern as current App.tsx placeholder)
   - Primary text: "Paste screenshot (Cmd/Ctrl+V) or drag image here"
   - Secondary text: "Supported formats: PNG, JPG, WebP"
   - When isDragging: Change text to "Drop image here"

3. **Preview state (capturedImage exists):**
   - Show `<img>` with `src={capturedImage.previewUrl}`
   - Alt text: "Captured screenshot"
   - Constrain size: max-w-full, max-h-64, object-contain
   - Center the image: mx-auto
   - Show image dimensions if available
   - "Clear image" button below preview (text-sm, text-muted-foreground, hover state)

4. **Error state:**
   - Show error message in text-red-600 text-sm
   - Position below the drop zone content
   - Error should auto-clear when user captures valid image

**Tailwind classes to use (matching existing theme):**
- text-muted-foreground for secondary text
- bg-secondary for subtle backgrounds
- rounded-md for buttons
- transition-colors for hover states

Follow the component pattern from 03-RESEARCH.md "ImageCapture Component Usage" section, but adapt styling to match existing App.tsx conventions.
  </action>
  <verify>
TypeScript compiles without errors:
```bash
pnpm tsc --noEmit
```
Verify component exports:
```bash
grep -E "export function ImageCapture" src/ui/components/ImageCapture.tsx
```
Verify hook import:
```bash
grep "useImageCapture" src/ui/components/ImageCapture.tsx
```
  </verify>
  <done>
ImageCapture.tsx exists with drop zone UI, preview display, error handling, clear button, and proper visual states for dragging/error. Uses useImageCapture hook. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ImageCapture into App.tsx</name>
  <files>src/ui/App.tsx</files>
  <action>
Replace the placeholder content in App.tsx with the ImageCapture component.

**Changes required:**

1. Add import at top of file:
```typescript
import { ImageCapture } from './components/ImageCapture';
```

2. Replace the main content area (the `<main>` section with placeholder icon and "Coming in Phase 3" text) with:
```tsx
<main className="flex-1 flex flex-col py-4">
  <ImageCapture />
</main>
```

3. Remove the placeholder SVG icon and "Coming in Phase 3" text - these are no longer needed.

**Keep unchanged:**
- Header section (title, status indicator)
- Footer section (selection count, ping button, close button)
- All message handling logic
- All state management for status, selectionCount, lastPing

The ImageCapture component handles its own state via the hook, so no additional state or handlers needed in App.tsx.
  </action>
  <verify>
TypeScript compiles without errors:
```bash
pnpm tsc --noEmit
```
Verify import exists:
```bash
grep "ImageCapture" src/ui/App.tsx
```
Build succeeds:
```bash
pnpm build
```
  </verify>
  <done>
App.tsx imports and renders ImageCapture component in main content area. Placeholder content removed. Build produces valid bundles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete image input functionality:
- Clipboard paste handler (Cmd/Ctrl+V)
- Drag-and-drop file handler
- Image preview display
- Format validation (PNG, JPG, WebP)
- Error display for invalid formats
- Clear image functionality
  </what-built>
  <how-to-verify>
1. Build the plugin: `pnpm build`
2. In Figma desktop, right-click canvas > Plugins > Development > Screenshot to Shadcn
3. Plugin should load with drop zone UI visible

**Test paste (INP-01):**
4. Take a screenshot (Cmd+Shift+4 on Mac, or use any PNG/JPG)
5. With plugin focused, press Cmd+V (or Ctrl+V on Windows)
6. Expected: Image preview appears in the drop zone

**Test drag-drop (INP-02):**
7. Clear the image if shown
8. Drag a PNG, JPG, or WebP file from Finder/Explorer onto the plugin window
9. Expected: Drop zone shows "Drop image here" while dragging
10. Expected: After drop, image preview appears

**Test validation:**
11. Try pasting or dropping a non-image file (e.g., .txt, .pdf)
12. Expected: Error message "Invalid format: ... Supported: PNG, JPG, WebP"

**Test clear:**
13. With image showing, click "Clear image" button
14. Expected: Returns to empty drop zone state
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all functionality works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks and checkpoint complete:

1. TypeScript compilation passes:
```bash
pnpm tsc --noEmit
```

2. Build succeeds:
```bash
pnpm build
```

3. All phase success criteria verified by human:
- [x] User can paste screenshot via Cmd/Ctrl+V and see it in plugin
- [x] User can drag-drop image file onto plugin window
- [x] Plugin displays preview of captured image before processing
- [x] Plugin validates image format (PNG, JPG, WebP) and shows error for invalid types
</verification>

<success_criteria>
- ImageCapture.tsx component renders drop zone with visual states
- App.tsx integrates ImageCapture, replacing placeholder
- Paste functionality works (Cmd/Ctrl+V captures image)
- Drag-drop functionality works (file drop captures image)
- Preview displays captured image
- Validation shows error for non-PNG/JPG/WebP files
- Clear button removes captured image
- Human verification confirms all functionality in Figma
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-input/03-02-SUMMARY.md`
</output>
